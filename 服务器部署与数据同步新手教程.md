# 质量分析平台：Linux服务器部署与数据同步新手教程

目标：你在网页里导入新数据后，其他同事刷新浏览器，就能看到更新后的数据。

服务器信息：

- 服务器 IP：`192.168.0.173`
- 服务器系统：Linux
- 登录方式：SSH
- 本机操作工具：Git Bash
- 访问方式：同事用浏览器打开内网地址

重要提醒：不要把服务器密码写进命令、脚本、文档或 GitHub。SSH 登录时输入密码即可。

---

## 1. 先理解：为什么只上传 dist 不够

当前 `dist` 是静态网页。

静态网页可以让同事打开看，但是数据通常保存在“当前浏览器本地”。

也就是说：

```text
你电脑导入数据
      ↓
只保存在你电脑浏览器本地
      ↓
同事刷新网页
      ↓
看不到你刚导入的数据
```

如果要做到“你导入新数据，同事刷新就看到”，必须让数据保存到服务器。

目标结构应该是：

```text
你在网页导入数据
      ↓
服务器保存最新数据
      ↓
同事刷新网页
      ↓
网页从服务器读取最新数据
```

所以最终需要：

```text
前端 dist
  +
Node.js 服务端接口
  +
服务器数据文件
```

---

## 2. 推荐服务器目录结构

Linux 服务器建议放在当前账号目录下：

```bash
~/QMSReport
```

最终目录结构：

```text
~/QMSReport
├─ app          # GitHub 下载的项目
├─ data         # 保存服务器共享数据
├─ logs         # 日志
└─ backups      # 数据备份
```

---

## 3. 用 Git Bash 登录服务器

在公司电脑打开 Git Bash。

输入：

```bash
ssh ym@192.168.0.173
```

第一次连接可能提示：

```text
Are you sure you want to continue connecting (yes/no/[fingerprint])?
```

输入：

```bash
yes
```

然后输入密码。

注意：输入密码时屏幕不会显示任何字符，这是正常的。

登录成功后，你就进入 Linux 服务器了。

---

## 4. 确认服务器系统

登录后输入：

```bash
uname -a
```

如果看到 Linux 字样，说明是 Linux 服务器。

查看当前目录：

```bash
pwd
```

查看当前目录文件：

```bash
ls
```

---

## 5. 创建部署目录

在服务器里输入：

```bash
mkdir -p ~/QMSReport/data
mkdir -p ~/QMSReport/logs
mkdir -p ~/QMSReport/backups
cd ~/QMSReport
```

确认目录：

```bash
pwd
ls
```

你应该能看到：

```text
data  logs  backups
```

---

## 6. 从 GitHub 下载项目

进入部署目录：

```bash
cd ~/QMSReport
```

下载项目：

```bash
git clone https://github.com/killsheepsman/Interim-Report.git app
```

进入项目目录：

```bash
cd ~/QMSReport/app
```

查看项目文件：

```bash
ls
```

应该能看到：

```text
package.json
src
public
...
```

---

## 7. 如果 git clone 失败怎么办

如果提示没有 git：

```bash
git --version
```

如果命令不存在，让 IT 安装 Git。

如果服务器是 Ubuntu/Debian，安装命令通常是：

```bash
sudo apt update
sudo apt install -y git
```

如果服务器是 CentOS/RHEL，安装命令通常是：

```bash
sudo yum install -y git
```

如果你没有 sudo 权限，把报错发给 IT。

---

## 8. 检查 Node.js 和 npm

输入：

```bash
node -v
npm -v
```

如果能显示版本号，说明已经安装。

如果提示命令不存在，让 IT 安装 Node.js LTS。

Ubuntu/Debian 常用安装方式：

```bash
sudo apt update
sudo apt install -y nodejs npm
```

但注意：系统自带 Node.js 可能版本较旧，建议 IT 安装 Node.js LTS 版本。

---

## 9. 安装项目依赖

进入项目目录：

```bash
cd ~/QMSReport/app
```

安装依赖：

```bash
npm install
```

如果 npm 很慢，可以切换国内镜像：

```bash
npm config set registry https://registry.npmmirror.com
npm install
```

---

## 10. 构建前端 dist

执行：

```bash
npm run build
```

成功后会生成：

```bash
~/QMSReport/app/dist
```

查看：

```bash
ls dist
```

应该能看到：

```text
index.html
assets
```

---

## 11. 临时预览 dist

如果只是临时看网页，可以执行：

```bash
npm run preview -- --host 0.0.0.0 --port 8080
```

然后同事访问：

```text
http://192.168.0.173:8080
```

但是注意：这个只是临时预览。

关闭 SSH 或命令窗口后，网页可能停止。

并且这仍然只是静态网页，不能实现多人数据同步。

---

## 12. 要实现数据同步，需要开发服务器同步模式

当前项目如果只运行 `dist`，仍然不能做到“你导入数据，同事刷新就看到”。

需要增加一个 Node.js 服务端，例如：

```text
server.js
```

它负责：

1. 提供网页：

```text
GET /
```

2. 提供最新数据：

```text
GET /api/data
```

3. 保存你导入的新数据：

```text
POST /api/data
```

4. 把数据保存到服务器目录：

```bash
~/QMSReport/data
```

同事刷新网页时，网页就读取服务器里的最新数据。

---

## 13. 推荐正式运行方式：PM2

PM2 可以让 Node.js 服务在后台运行。

安装 PM2：

```bash
npm install -g pm2
```

如果没有权限，试：

```bash
sudo npm install -g pm2
```

如果没有 sudo 权限，让 IT 安装。

---

## 14. 后续开发完成 server.js 后，启动服务

进入项目目录：

```bash
cd ~/QMSReport/app
```

启动：

```bash
pm2 start server.js --name qms-report
```

查看状态：

```bash
pm2 status
```

查看日志：

```bash
pm2 logs qms-report
```

重启：

```bash
pm2 restart qms-report
```

停止：

```bash
pm2 stop qms-report
```

---

## 15. 设置 PM2 开机自启动

执行：

```bash
pm2 startup
```

它会输出一行命令，通常以 `sudo` 开头。

复制它输出的那行命令并执行。

然后保存当前 PM2 服务：

```bash
pm2 save
```

如果没有权限，让 IT 操作这一步。

---

## 16. 防火墙和端口

如果服务运行在 `8080` 端口，同事访问：

```text
http://192.168.0.173:8080
```

如果打不开，让 IT 检查服务器防火墙是否放行 `8080`。

Ubuntu 常见命令：

```bash
sudo ufw allow 8080/tcp
sudo ufw status
```

CentOS/RHEL 常见命令：

```bash
sudo firewall-cmd --add-port=8080/tcp --permanent
sudo firewall-cmd --reload
```

如果没有 sudo 权限，让 IT 放行端口。

---

## 17. 更新项目代码

以后你在本机开发完成并上传 GitHub 后，服务器更新：

用 Git Bash 登录服务器：

```bash
ssh ym@192.168.0.173
```

进入项目目录：

```bash
cd ~/QMSReport/app
```

拉取最新代码：

```bash
git pull
```

安装依赖：

```bash
npm install
```

重新构建：

```bash
npm run build
```

重启服务：

```bash
pm2 restart qms-report
```

---

## 18. 更新数据后的使用流程

开发服务器同步模式后，使用流程应该是：

1. 你打开：

```text
http://192.168.0.173:8080
```

2. 登录管理员账号。
3. 进入“数据导入”页面。
4. 上传新的 IQC / IPQC / OQC / DQA 数据。
5. 点击完成导入。
6. 系统把新数据保存到服务器。
7. 同事刷新浏览器。
8. 同事看到最新数据。

---

## 19. 权限控制建议

建议做两类账号。

### 管理员账号

可以：

- 导入数据；
- 保存数据；
- 修改映射表；
- 修改批注；
- 导出报告。

### 查看账号

只能：

- 查看图表；
- 切换页面；
- 导出报告。

不能：

- 导入数据；
- 删除数据；
- 修改映射表；
- 修改批注。

---

## 20. 常见问题

### 20.1 ssh 登录失败

命令：

```bash
ssh ym@192.168.0.173
```

如果失败，找 IT 确认：

- SSH 服务是否开启；
- 22端口是否开放；
- 账号是否允许 SSH 登录；
- 密码是否正确。

### 20.2 powershell command not found

这是正常的。

因为服务器是 Linux，不是 Windows。

Linux 下不要输入：

```bash
powershell
```

应该使用：

```bash
mkdir
cd
ls
pwd
git
npm
pm2
```

### 20.3 npm install 很慢

切换镜像：

```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### 20.4 同事打不开网页

检查：

1. 服务是否启动：

```bash
pm2 status
```

2. 端口是否监听：

```bash
ss -lntp | grep 8080
```

3. 防火墙是否放行；
4. 浏览器地址是否正确：

```text
http://192.168.0.173:8080
```

### 20.5 数据导入后同事没变化

检查：

1. 项目是否已经开发服务器同步模式；
2. 数据是否保存到服务器；
3. 同事是否刷新浏览器；
4. 可以让同事按：

```text
Ctrl + F5
```

强制刷新。

---

## 21. 当前结论

如果只是运行 `dist`：

```text
只能让同事查看固定数据。
```

如果要实现：

```text
你导入新数据，同事刷新同步看到
```

必须增加：

```text
Node.js 服务端 + 数据保存接口 + 登录权限
```

建议下一步开发：

1. 服务器同步数据接口；
2. 管理员/查看账号登录；
3. 数据导入只允许管理员操作。

