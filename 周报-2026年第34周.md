# QMS质量分析平台开发周报

**周期：2026年8月17日—2026年8月23日**  
**项目：quality-analytics-web**

## 一、本周工作概况

本周重点围绕质量分析 Agent、角色 Agent 报告、后台快照和报告趋势规则进行优化，目标是减少页面卡顿、固定统计口径、提高报告可复用性，并保证管理员能够控制快照生成和报告调用。

## 二、本周完成事项

### 1. 后台快照体系建设

- 完成模块快照和角色快照的统一生成入口。
- 支持 IQC、IPQC、OQC、DQA、QMS 五个模块快照。
- 支持组装人员、机长、交付经理、供应链经理、研发工程师、PM、TPM、产总角色快照。
- 快照支持总周期、月度、周度周期拆分。
- 增加任务进度、当前处理状态、停止任务、失败任务重试。
- 增加快照历史、启用/停用、删除、备注和版本管理。
- 增加批次标识，便于识别同一次生成产生的总周期、月度和周度快照。
- 角色快照同步纳入同一套周期和批次管理规则。

### 2. 后台快照页面性能优化

- 发现页面卡顿主要来自一次性读取和渲染完整快照正文。
- 增加快照索引模式：页面先读取模块、周期、状态、批次、人数和摘要等轻量信息。
- 快照正文改为点击“查看”时按需读取。
- 历史列表增加滚动区域，避免大量记录撑长页面。
- 保留 Agent 调用完整快照的能力，不改变报告统计结果。

### 3. 快照生成按钮和权限修复

- 修复生成按钮因权限状态延迟导致原生禁用、点击无反应的问题。
- 按钮在点击后立即显示“正在准备数据”。
- 兼容 `isAdmin/isDeputy` 和 `role=admin/deputy` 两种权限字段。
- 未授权账号点击后明确提示权限不足。
- 增加 `type="button"`，避免外层表单拦截点击事件。

### 4. 角色 Agent 报告趋势规则

- 修复半年报告只显示最近两周的问题。
- 统一要求跨月报告完整输出统计范围内所有自然月。
- 统一要求跨周报告完整输出起止日期之间的连续周。
- 无记录月份或周次保留并填 0，不再只显示有异常的周期。
- 趋势图统一使用两组柱形加一条折线：不良数量、总数量、不良率。
- 该规则已提升到通用 `quality-analysis-core`，适用于所有模块和角色报告。

### 5. Skill 和视觉契约完善

- 更新 `quality-analysis-core` 的跨月、跨周趋势规则。
- 更新 `report-visual-contract` 的趋势图表契约。
- 角色报告生成器增加完整趋势序列要求，避免模型自行截断趋势。
- 保持图表、表格、报告 Profile 与统计数据分离，避免换排版影响数字口径。

### 6. 工程与验证

- 多次执行 `npm.cmd run build`，当前构建成功。
- 对服务端状态接口、快照注册表接口进行了检查。
- 对关键文件执行语法和 `git diff --check` 检查。

## 三、当前问题与风险

1. 后台快照历史数据较多时，生成过程仍可能耗时较长，需要继续观察真实服务器上的数据加载和解析耗时。
2. 历史快照数量较大时，仍需进一步完善按模块、角色、周期和批次的折叠展示，避免用户在列表中逐条查找。
3. 当前工作区存在较多未提交改动，发布前需要统一检查、提交并重新构建部署包。
4. 服务器部署时必须确认使用最新 `dist`，并执行浏览器强制刷新，避免旧构建缓存造成“按钮无效”假象。

## 四、下周计划

### 优先级 P0：快照生成稳定性

- 继续定位“点击生成快照无反应”问题，增加任务开始、数据加载、周期计算、写入数据库四阶段状态。
- 将生成任务改为分批提交和分段保存，避免一次任务长时间占用浏览器主线程。
- 对失败周期单独记录并支持只重试失败项。

### 优先级 P1：快照管理体验

- 报告快照按 IQC、IPQC、OQC、DQA、QMS 分组展示。
- 角色快照按供应链角色链和研发角色链分组展示。
- 增加总周期/月度/周度折叠层级。
- 增加批次级全选、启用、停用和删除操作。

### 优先级 P1：Agent 调用优化

- Agent 优先读取与报告周期精确匹配的有效快照。
- 没有精确快照时明确提示，不自动混用其它周期数据。
- 将快照命中情况显示在 Agent 页面，方便管理员核查。

### 优先级 P2：发布与部署

- 整理本周所有改动并提交 Git。
- 重新生成生产构建。
- 在服务器上验证 PostgreSQL、状态接口、快照生成和报告调用。
- 形成正式发布版本和回滚版本。

## 五、本周结论

本周已完成从“Agent 临时计算”向“后台固定快照 + Agent 分析”的基础转型。当前重点已从功能开发转向快照生成稳定性、历史管理效率和服务器现场验证。下周优先保证快照生成过程可见、可恢复、可重试，再继续完善批量管理和 Agent 精确调用。
