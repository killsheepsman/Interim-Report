# QMS质量分析平台开发周报

**周期：2026年9月1日—2026年9月5日**  
**项目：quality-analytics-web**

## 一、本周工作概况

本周围绕“证据生成 V2 + 真实样本验收”继续推进知识库能力，重点处理不同格式规范导入后的证据质量、异常大文件后台任务、知识库首屏加载和删除可靠性问题。通过对旧版 DOC、DOCX、PDF、XLS 等实际样本进行读取和抽样，确认了当前解析链路的主要缺陷，并完成了第一轮规则和任务控制修复。

## 二、本周完成事项

### 1. 证据生成规则升级

- 增加证据元数据过滤，排除“文件类型、设计规范、版本、封面、目录、审核、批准”等非规范要求内容。
- 过滤孤立数字、单位、型号、单词和标题碎片，避免生成无语义证据。
- 增加二进制/乱码文本识别，避免旧版 DOC 被误解析为大量乱码证据。
- PDF 长页面不再整页合并，改为按行和语句边界拆分，并保留页码、来源和 OCR 状态。
- 保留连续逐字原文、章节位置、文件哈希和来源信息，为后续知识卡引用提供依据。

### 2. 多格式真实样本验收

完成以下样本的导入和证据抽样：

| 文件类型 | 样本 | 当前结果 |
|---|---|---|
| 旧版 DOC | DOCK 防水测治具调试手册 | 新导入版本已解析 164 条；旧版异常文件识别为二进制文本问题 |
| DOCX | QS-QP07-EN06 气路走线通用规范 | 已解析 55 条，正文规则基本完整 |
| PDF | SMC 单直通、直通型调速阀安装规范 | 已解析 3 条，原生文字与 OCR 路由生效 |
| XLS | 螺钉通用工艺规范 | 已解析 27 条，包含表格规则和锁紧要求 |

验收发现：DOCX 和 XLS 的有效规则比例较高；PDF 仍需进一步拆分图示、页眉和正文；旧版 DOC 需优先识别文件编码/容器类型，不能直接按普通文本解析。

### 3. XLS 解析失败修复

- 定位 `QS-QP07-05.18 公司所有设备螺丝锁紧及选用规范-V2.0.xls` 失败原因：服务端错误调用 Python 提取器，而该提取器不支持 `.xls`。
- 改为调用项目已有的 Node `xlsx` 解析脚本处理 `.xls/.xlsx/.xlsm`。
- 保留工作表名称、行号和原始单元格内容，继续交由证据规则进行表头、行政信息和规则行过滤。

### 4. 大文件后台任务控制

- 处理中任务允许直接删除，不再要求先等待解析完成。
- 删除时取消队列任务和关联控制器，后台结果不会再写回已删除文档。
- 删除文档时同步取消关联解析/蒸馏任务。
- 为外部解析子进程接入取消信号，避免页面删除后后台仍继续消耗资源。
- 增加任务暂停、继续控制的服务端分支，解析任务状态可以回到等待队列。

### 5. 55MB 异常 DOC 残留清理

- 确认旧版 `QS-QP07-01.05 DOCK防水测治具调试手册--V1.0.doc` 曾写入旧 JSON 快照，导致快照膨胀至约 409MB。
- 按旧文档 ID 清理文档和关联等待任务。
- 清理后旧快照由约 409MB 降至约 18.5MB。
- 保留清理前备份：`data/knowledge-store.json.before-delete-e9e667b7.bak`。
- 当前页面中同名文件为重新导入的新记录，大小约 8.47MB，与旧记录不是同一条数据。

### 6. 知识库首屏和服务启动优化

- 首屏文档索引不再为每个文件加载全部知识卡片，知识卡改为打开复核时按需分页读取。
- 启动脚本恢复使用轻量 `/api/me` 健康检查，避免启动时读取完整知识库造成长时间等待。
- 知识库请求增加短暂断线自动重试。

## 三、本周验证结果

- 服务端 `node --check server/knowledgeService.mjs` 通过。
- 前端 `npm.cmd run build` 通过。
- `git diff --check` 通过。
- 已验证知识库中的 DOC、DOCX、PDF、XLS 样本状态和证据数量。
- 已验证旧 55MB 文档 ID 不再出现在当前知识库索引中。
- 已确认 `.xls` 解析失败为解析器路由问题，而非文件本身不可读。

## 四、当前问题与风险

1. 旧版 DOC 的文本恢复仍依赖文件实际编码和 OLE 结构，当前只能识别并阻止乱码污染，不能保证完整恢复原始排版。
2. PDF 中图片主导的页面仍需要 OCR 图像证据和人工复核，当前文字证据不能代表图标、箭头和位置关系。
3. 部分 DOCX 证据仍包含职责说明、目录标题和操作章节标题，需要进一步区分“背景说明”和“可执行规范”。
4. XLS 表格中的跨行合并、表头继承和图片内容仍需要专项规则，不能只依赖单行文本。
5. 当前工作区仍有未提交的解析、任务控制和清理脚本改动，需要完成一次整体回归后再提交。

## 五、下周计划

### 优先级 P0：证据质量继续验收

- 对重新导入的 4 份样本逐条抽样，统计有效证据率、元数据误收率、碎片率和重复率。
- 为 PDF 图示页建立“文字证据 + 图片证据 + 人工复核状态”的统一结构。
- 为 DOCX 增加职责、目录、标题和规范要求的分类规则。

### 优先级 P1：异常文件处理

- 对旧版 DOC 增加文件容器识别和可恢复性判断。
- 对无法可靠读取的文件在导入时直接标记“需转换或人工复核”，不进入知识蒸馏。
- 增加任务中心的解析任务暂停、继续、删除按钮和状态提示的完整回归。

### 优先级 P1：知识卡质量验证

- 使用清洗后的证据重新生成知识卡。
- 检查知识卡是否包含完整陈述、适用范围、正确状态、常见违规表现和原文来源。
- 禁止标题、孤立数字和不完整表格行进入正式知识卡。

### 优先级 P2：数据一致性与发布

- 检查 PostgreSQL、JSON 兼容快照和前端索引的文档数量、证据数量和知识卡数量是否一致。
- 完成解析器和后台任务改动的回归测试。
- 整理本周改动并形成可回滚提交。

## 六、本周结论

本周完成了证据生成 V2 的第一轮真实样本验证，确认系统已经能够稳定处理 DOCX、PDF 和 XLS 的主要文字内容，但旧版 DOC、图片主导 PDF 和复杂表格仍不能仅依赖普通文本提取。后台任务方面，删除、取消和首屏加载问题已经完成核心修复，下一步应把工作重点放在证据有效率量化、图文证据关联和知识卡质量验收，而不是继续扩大功能范围。
