# 质量分析 Agent 开发计划

## 目标

在不改变现有原始数据导入、固定统计分析、质量报告和知识考试功能的前提下，新增“质量分析 Agent”能力。Agent 只解释固定统计引擎已经计算出的结果，不负责重新计算指标。

## 分层边界

### 固定软件层

- Excel/CSV 导入、字段识别和数据源管理
- 日期周期、指标口径、同比、密度、排名和二八计算
- 产品部、TPM、PM、研发工程师、工坊、机长映射
- 知识库、题库、考试链接和考试结果存储
- 原有 AI分析、质量报告和报告任务中心

### Agent 层

- 数据审计解释
- 结果、过程和根因分析
- 风险分级和责任层级判断
- 30/60/90 天改善行动
- 证据不足提示和人工审核建议
- 研发问题、ECN、设计评审与跨模块关联

## 交付阶段

1. `quality-agent-snapshot-v1`：从 `analyzeImported()` 输出生成模块分析快照。
2. `quality-agent-v1`：实现 Agent 审计、二八分析、责任行动、正式报告四阶段，并支持断点继续。
3. DQA Agent：研发问题、ECN、设计评审和非 BOM 四类证据接入。
4. IQC/IPQC/OQC/QMS Agent：复用相同协议，替换模块规则。
5. 角色 Agent：公司、产品部、TPM、PM、研发工程师、供应链和IPQC角色报告。
6. 跨模块 Agent：DQA→IPQC→OQC→QMS质量链关联，未经标识符验证只能标记为假设。
7. 反馈闭环：改善行动、考试结果、重复问题和验证证据回写报告。

## 当前实现

- 新增 `src/agent/qualitySnapshot.js`
- 新增 `src/agent/qualityAgent.js`
- 新增 `src/agent/QualityAgentPage.jsx`
- “质量分析 Agent”常驻菜单，菜单显示与“启动 Agent 分析”功能权限分开控制
- 新入口名称固定为“质量分析 Agent”
- 现有固定统计和角色报告功能保持独立，不因 Agent 分析失败而失效
- Agent 报告可保存到 `outputs/ai_saved_reports`
- Agent角色报告页面可创建带角色、收件人和报告文件的“待发送”任务

### 本轮补充

- Agent 报告保存和发送任务使用同一个实际返回的报告文件路径，避免任务指向旧文件。
- 新增 `GET /api/ai/agent-dispatch`，可读取服务器上的 Agent 发送任务记录。
- Agent 页面和“报告任务中心”增加独立的 Agent 发送任务记录，与原质量报告任务分开。
- IQC、IPQC、OQC、QMS 增加模块化固定快照，提供对应指标、组织对象、趋势和问题证据；模型仍不能重新计算软件指标。
- DQA 产品部/TPM 选择会按已有组织字段过滤；PM/研发工程师若缺少专用映射字段会显示“待核实”，不会把 TPM 数据冒充个人数据。
- Skill 目录固定为 `skills/`，页面通过 `/api/ai/skills` 自动读取其中的 `SKILL.md` 并生成下拉选项。
- 原始 Agent Markdown 通过项目报告库保存到 `outputs/ai_saved_reports`；角色报告和发送任务独立于原始 Agent 分析页，考试结果独立统计。
- 通用规则已拆分为 `quality-analysis-core` 与 IQC/IPQC/OQC/DQA/QMS/跨模块 Skill；运行时自动组合核心与当前模块 Skill。
- 固定快照升级为 `quality-agent-snapshot-v3`，增加 A/B/C/D 数据审计、本地双重 Pareto 和 S/M/O/C/X 标准证据目录。
- 优先结论使用 K 编号证据卡；正式报告缺少有效证据引用时自动追加发布限制。
- 改善行动解析为本地结构化台账，保存基线、目标、复查日期、关闭证据和重开阈值；达到目标后仍需人工核验关闭，后续指标反弹时自动重开。

## 验收标准

- Agent 报告数字与总览数字一致。
- 报告能显示数据周期、来源和缺失字段。
- Agent 失败后可以从失败阶段继续。
- 每个风险都有证据或明确标记“待核实”。
- 每个改善行动都有责任对象、期限、指标和关闭条件。
- 服务器保存报告后能返回可追踪的相对路径。
- 发送任务必须明确角色和收件人；企业微信实际推送单独验收。
- 关闭权限设置中的 `qualityAgent` 后，只有管理员/副管理员仍可访问 Agent；现有项目页面和流程保持原状。
